services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:////data/travel_planner.db
    volumes:
      - travel_planner_data:/data

volumes:
  travel_planner_data:
